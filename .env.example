# Anthropic (Claude) — LLM
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Public URL where this Next.js app is reachable (used for QR code links).
# Both the Next.js frontend and the Pipecat voice server read this value.
# If omitted, defaults to window.location.origin (web) or http://localhost:3000 (voice).
# NEXT_PUBLIC_URL=https://your-kiosk.example.com
